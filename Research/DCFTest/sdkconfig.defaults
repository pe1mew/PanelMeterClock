# 1 kHz FreeRTOS tick (ESP-IDF defaults to 100 Hz) so pdMS_TO_TICKS()-based
# pacing in the dcf77 decoder (edge flush, watchdog) keeps ms granularity.
CONFIG_FREERTOS_HZ=1000

# LOLIN S3 has 16 MB flash (IDF defaults to 2 MB).
CONFIG_ESPTOOLPY_FLASHSIZE_16MB=y
